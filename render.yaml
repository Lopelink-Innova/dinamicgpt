services:
  - type: web
    name: mini-chat-gemini
    env: python
    plan: free
    buildCommand: "pip install -r requirements.txt"
    startCommand: "python app.py"
    envVars:
      - key: GOOGLE_API_KEY
        value: tu_clave_api_google
